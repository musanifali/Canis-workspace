{
  "name": "@workspace-engine/dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Canis vendor dashboard (#31) — and the dogfooding proof (#53): its analytics views are validated workspaces saved in the Phase 4 service, rendered by the public SDK surface only.",
  "scripts": {
    "dev": "next dev -p 3002",
    "build": "next build",
    "start": "next start -p 3002",
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed": "tsx scripts/seed.mts"
  },
  "dependencies": {
    "@workspace-engine/client": "0.1.0",
    "@workspace-engine/core": "0.1.0",
    "@workspace-engine/react": "0.1.0",
    "@workspace-engine/ui": "0.1.0",
    "next": "^15.5.7",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
