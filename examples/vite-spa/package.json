{
  "name": "@workspace-engine/example-vite-spa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vite SPA example (#50): contract + validated spec + renderer in a plain client-side React app. CI builds it against the current packages — it feeds the compat suite (#51).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@workspace-engine/core": "0.1.0",
    "@workspace-engine/react": "0.1.0",
    "@workspace-engine/ui": "0.1.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
