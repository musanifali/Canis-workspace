{
  "name": "@workspace-engine/example-next-app",
  "version": "0.1.0",
  "private": true,
  "description": "Next.js App Router example (#50): the RSC/client boundary drawn explicitly — server layout + data route, client workspace module. CI builds it against the current packages — it feeds the compat suite (#51).",
  "scripts": {
    "dev": "next dev -p 3004",
    "build": "next build",
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@workspace-engine/core": "0.1.0",
    "@workspace-engine/react": "0.1.0",
    "@workspace-engine/ui": "0.1.0",
    "next": "^15.5.7",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/react": "^19",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
