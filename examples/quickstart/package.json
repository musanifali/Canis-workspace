{
  "name": "@workspace-engine/example-quickstart",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The docs quickstart, as real compiled+tested code. Every fenced code block in apps/docs' quickstart page is one of these files — quickstart-sync.test.ts fails CI if they drift.",
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@workspace-engine/core": "0.1.0",
    "@workspace-engine/react": "0.1.0",
    "@workspace-engine/ui": "0.1.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/react": "^19",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
