{
  "name": "@ticora/example-recipes",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "The adapter recipes from the docs (REST/GraphQL/Prisma/Supabase fetch implementations), as compiled + unit-tested code. The docs' adapters page syncs against these files.",
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@ticora/core": "0.3.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
