# the vendored Tambo clone at the repo root ONLY (anchored: demo/src/components/tambo must stay tracked)
/tambo/
node_modules/
dist/
.next/
.turbo/
*.tsbuildinfo
.env.local
.env
.env.mcp
*.log
.DS_Store
# Obsidian vault is versioned; ignore only per-machine editor state
vault/.obsidian/workspace.json
vault/.obsidian/workspace-mobile.json
vault/.obsidian/cache
